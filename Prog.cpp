#include <iostream>
#include "funcs.h"
using namespace std;
int main()
{
	double a, b;
    char c;
    cout << "Input expression like: a*b, * as C; C can be '*','/','+','-'" << endl;
    cin >> a;
    cin >> c;
    cin >> b;

    }
system("pause");
return 0;
}